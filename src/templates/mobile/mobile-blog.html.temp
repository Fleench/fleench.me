<!DOCTYPE html>
<!-- template: mobile-blog -->
<html lang="en">
  <head>
    ~{src/elements/head.element}~
    <link rel="alternate" type="application/rss+xml" title="Site RSS" href="/rss.xml" />
    <link rel="stylesheet" href="/css/mobile.css" />
  </head>

  <body>
    <div class="page-wrap">
      <div class="site-layout">
        <div class="center-column">
          <div class="content-shell">
            ~{src/elements/m-nav.element}~

            <div class="content-row">
              <main class="layout h-entry">
                <section class="content panel">
                  <div class="blog e-content">
                    <h1>{{ title }}</h1>
                    <img alt="{{ img-alt }}" src="{{ img }}" />
                    {{ content }}
                  </div>
                  <div style="display:none;">
                    <a class="u-url p-name" href="{{ output }}">{{ title }}</a>
                    <time class="dt-published" datetime="{{ date }}">{{ date }}</time>
                    <div class="p-author h-card">
                      <a class="u-url n" href="https://flench.me">Glenn Sutherland</a>
                    </div>
                  </div>
                </section>

                <section class="main-extras">
                  <div id="webmentions"></div>
                  ~{src/elements/footer.element}~
                </section>
              </main>
            </div>
          </div>
        </div>
      </div>
    </div>
  </body>
</html>
